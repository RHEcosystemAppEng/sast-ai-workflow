# Local Langfuse Configuration Template
# Copy this to .env.langfuse.local and fill in your API keys

# Langfuse API Keys (generate at http://localhost:3000 -> Settings -> API Keys)
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# Langfuse Host (local Docker Compose instance)
LANGFUSE_HOST=http://localhost:3000

# How to get API keys:
# 1. Start Langfuse: docker-compose -f docker-compose.langfuse.yml up -d
# 2. Open http://localhost:3000 in browser
# 3. Create an account (first time only)
# 4. Create a project (first time only)
# 5. Go to Settings -> API Keys
# 6. Click "Create new API key"
# 7. Copy the public key (pk-lf-...) and secret key (sk-lf-...)
# 8. Paste them above and save as .env.langfuse.local
